# models


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## VerifyStepState

TypedDict for Phase 4: Verify step state.

------------------------------------------------------------------------

### VerifyStepState

``` python

def VerifyStepState(
    args:VAR_POSITIONAL, kwargs:VAR_KEYWORD
):

```

*State for Phase 4: Verify.*

## SegmentSample

Lightweight segment data for sample display.

------------------------------------------------------------------------

### SegmentSample

``` python

def SegmentSample(
    index:int, text:str, start_time:Optional=None, end_time:Optional=None
)->None:

```

*Lightweight segment data for sample display.*

## VerificationResult

Complete verification data computed from graph database queries.

------------------------------------------------------------------------

### VerificationResult

``` python

def VerificationResult(
    document_id:str, document_title:str, document_media_type:str, segment_count:int, total_duration:float,
    avg_segment_duration:float, has_starts_with:bool, starts_with_count:int, next_chain_complete:bool,
    next_count:int, part_of_complete:bool, part_of_count:int, all_have_timing:bool, segments_missing_timing:int,
    all_have_sources:bool, segments_missing_sources:int, source_plugins:List=<factory>,
    first_segments:List=<factory>, last_segments:List=<factory>
)->None:

```

*Complete verification data from graph database queries.*

## VerifyUrls

URL bundle for Phase 4 verify route handlers.

------------------------------------------------------------------------

### VerifyUrls

``` python

def VerifyUrls(
    verify:str='', sample:str=''
)->None:

```

*URL bundle for Phase 4 verify route handlers.*
